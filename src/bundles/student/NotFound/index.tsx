import * as React from 'react'

import './component.less'

const NotFound: React.StatelessComponent<{}> = (): JSX.Element => (
	<section id='not-found'>
		<h1>Not found</h1>
	</section>
)

export default NotFound
