import * as React from 'react'

import './component.less'

const Students: React.StatelessComponent<{}> = (): JSX.Element => (
	<section id='students'>
		<h1>Students</h1>
	</section>
)

export default Students
